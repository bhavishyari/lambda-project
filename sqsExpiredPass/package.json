{
  "name": "sqs-expired-pass",
  "version": "1.0.0",
  "description": "SQS message handler to change status of boarding pass to EXPIRED.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^1.78.1",
    "serverless-offline": "^6.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.524.0",
    "node-fetch": "^2.6.0"
  }
}
