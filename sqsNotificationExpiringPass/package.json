{
  "name": "sqs-notification-expiring-pass",
  "version": "1.0.0",
  "description": "SQS message handler to notification for expiring pass.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^1.78.1",
    "serverless-offline": "^6.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.524.0",
    "dotenv": "^8.1.0",
    "node-fetch": "^2.6.0",
    "moment": "^2.27.0",
    "moment-timezone": "^0.5.31",
    "validator": "^11.1.0"
  }
}
