{
  "name": "sqs-send-push",
  "version": "1.0.0",
  "description": "SQS message handler to send push notification using FCM",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^1.78.1",
    "serverless-offline": "^6.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.524.0",
    "dotenv": "^8.1.0",
    "firebase-admin": "^8.13.0",
    "node-fetch": "^2.6.1",
    "pushwoosh-client": "^1.5.0",
    "validator": "^11.1.0"
  }
}
