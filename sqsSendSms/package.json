{
  "name": "sqs-send-sms",
  "version": "1.0.0",
  "description": "SQS message handler to send sms",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^1.78.1"
  },
  "dependencies": {
    "aws-sdk": "^2.524.0",
    "dotenv": "^8.1.0",
    "node-fetch": "^2.6.1",
    "validator": "^11.1.0"
  }
}
